import ProfileCard from "../components/ProfileCard";

function Profile() {
  window.scrollTo(0, 0);

  return (
    <div className="profile container">
      <ProfileCard />
    </div>
  );
}

export default Profile;
